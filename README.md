# deno-react-mantine-template

Deno is a single binary executable, running in user space with no external dependencies.

Install it locally with `curl -fsSL https://deno.land/install.sh | sh` or dwnload the executable in the folder.

Initialize the project with `deno i`.

Run the local server with `deno task dev`.
